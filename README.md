# Generic bytecode interpreter

Go to `tests` to see how to use it.

Features (will change a lot):

* Generic "processor" with stack and stack pointer
* Simple commands without arguments
* Interpreter